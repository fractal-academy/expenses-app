import React from 'react'
import { render } from '@testing-library/react'
import NotificationSimpleView from '../NotificationSimpleView.template'

test('Render NotificationSimpleView test.', () => {
  render(<NotificationSimpleView />)
})
