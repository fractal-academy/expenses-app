import SessionLogin from './SessionLogin.layout'

const metadata = {
  title: 'domains/Session/routes/SessionLogin',
  component: SessionLogin
}
export default metadata

const Template = (args) => <SessionLogin {...args} />

export const SessionLoginStory = Template.bind({})

SessionLoginStory.args = {}
