import CategoryAll from './CategoryAll.layout'

const metadata = {
  title: 'domains/Category/routes/CategoryAll',
  component: CategoryAll
}
export default metadata

const Template = (args) => <CategoryAll {...args} />

export const CategoryAllStory = Template.bind({})

CategoryAllStory.args = {}
