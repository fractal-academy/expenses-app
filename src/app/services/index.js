import {
  setData,
  getData,
  firestore,
  getCollectionRef,
  getDataWithFilter,
  getOrderedData
} from './Firestore'
import { upload, getURL, deleteURL } from './Storage'
import { firebase } from './Firebase'

export {
  setData,
  getData,
  firestore,
  firebase,
  upload,
  getURL,
  deleteURL,
  getCollectionRef,
  getDataWithFilter,
  getOrderedData
}
