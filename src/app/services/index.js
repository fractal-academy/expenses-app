import { setData, getData, customStore } from './Store'
import { upload, getURL, deleteURL } from './Storage'
import { firebase } from './Firebase'

export { setData, getData, customStore, firebase, upload, getURL, deleteURL }
