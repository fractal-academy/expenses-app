import {
  setData,
  getData,
  customStore,
  getCollectionRef,
  getDataWithFilter
} from './Store'
import { upload, getURL, deleteURL } from './Storage'
import { firebase } from './Firebase'

export {
  setData,
  getData,
  customStore,
  firebase,
  upload,
  getURL,
  deleteURL,
  getCollectionRef,
  getDataWithFilter
}
