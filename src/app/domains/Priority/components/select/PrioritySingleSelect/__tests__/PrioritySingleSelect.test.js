import React from 'react'
import { render } from '@testing-library/react'
import PrioritySingleSelect from '../PrioritySingleSelect.template'

test('Render PrioritySingleSelect test.', () => {
  render(<PrioritySingleSelect />)
})
