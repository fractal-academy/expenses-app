import LogAll from './LogAll.layout'

const metadata = {
  title: 'domains/Log/routes/LogAll',
  component: LogAll
}
export default metadata

const Template = (args) => <LogAll {...args} />

export const LogAllStory = Template.bind({})

LogAllStory.args = {}
