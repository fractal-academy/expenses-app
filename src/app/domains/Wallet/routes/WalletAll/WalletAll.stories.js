import WalletAll from './WalletAll.layout'

const metadata = {
  title: 'domains/Wallet/routes/WalletAll',
  component: WalletAll
}
export default metadata

const Template = (args) => <WalletAll {...args} />

export const WalletAllStory = Template.bind({})

WalletAllStory.args = {}
