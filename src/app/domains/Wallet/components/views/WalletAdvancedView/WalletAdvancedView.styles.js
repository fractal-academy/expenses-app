import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles({
  styledPaper: {
    borderRadius: '0.5rem',
    overflow: 'hidden'
  },
  bold: {
    fontWeight: 600,
    color: '#ffffff'
  }
})
