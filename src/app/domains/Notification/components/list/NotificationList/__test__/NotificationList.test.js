import React from 'react'
import { render } from '@testing-library/react'
import NotificationList from '../NotificationList.template'

test('Render NotificationList test.', () => {
  render(<NotificationList />)
})
