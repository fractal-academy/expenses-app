import Modal from './Modal.template'

const metadata = {
  title: 'domains/Measure/combined/Modal',
  component: Modal
}
export default metadata

const Template = (args) => <Modal {...args} />

export const ModalStory = Template.bind({})

Modal.args = {}
