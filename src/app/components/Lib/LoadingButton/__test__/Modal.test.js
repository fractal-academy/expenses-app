import React from 'react'
import { render } from '@testing-library/react'
import LoadingButton from '../LoadingButton.template'

test('Render LoadingButton test.', () => {
  render(<LoadingButton />)
})
