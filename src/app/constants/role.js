const ROLE = { admin: 'admin', user: 'user', observer: 'observer' }

const ROLE_KEYS = Object.keys(ROLE)
const ROLE_VALUES = Object.values(ROLE)

export default ROLE
export { ROLE_KEYS, ROLE_VALUES }
