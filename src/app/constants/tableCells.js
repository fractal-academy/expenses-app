const CART_CELLS = ['Product', 'Assigned', 'Category']

const WISHES_CELLS = ['Wish', 'Assigned', 'Category']

const REGULAR_PRODUCT_CELLS = ['Product', 'Assigned', 'Category']

const PURCHASES_CELLS = ['Product', 'Who bought', 'Quantity', 'Price']

export { CART_CELLS, WISHES_CELLS, REGULAR_PRODUCT_CELLS, PURCHASES_CELLS }
